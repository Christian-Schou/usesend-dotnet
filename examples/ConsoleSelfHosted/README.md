# ConsoleSelfHosted Example

Demonstrates connecting the useSend .NET SDK to a **self-hosted** useSend instance by setting a custom `ApiUrl`.

## Run

```bash
export USESEND_API_KEY=us_your_api_token
dotnet run
```

Update the `ApiUrl` in `Program.cs` to match your self-hosted instance URL.
